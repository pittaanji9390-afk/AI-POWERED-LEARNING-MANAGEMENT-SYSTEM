# Operations, Runbooks & Observability

## Monitoring & Metrics
- Health checks: `/api/v1/health` and Spring Boot `/actuator/health`.
- Prometheus metrics: `/actuator/prometheus`.
- Structured JSON logging with `X-Request-ID` tracing across all layers.
